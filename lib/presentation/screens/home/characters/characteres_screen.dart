import 'package:flutter/material.dart';

class CharacteresScreen extends StatelessWidget {
  const CharacteresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Characters'),
    );
  }
}
